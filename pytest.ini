[pytest]
# Ne collecter que les vrais tests unitaires (les scripts d'intégration
# à la racine / dans scripts/ ne sont pas des tests et nécessitent API + données).
testpaths = backend/tests
pythonpath = backend
